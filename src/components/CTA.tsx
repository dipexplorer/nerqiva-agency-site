"\"use client\";\n\nimport { useState } from \"react\";\n\nconst FIELDS = [\n  { id: \"name\", label: \"Your Name\", type: \"text\", placeholder: \"Alex Johnson\" },\n  { id: \"email\", label: \"Email Address\", type: \"email\", placeholder: \"alex@company.com\" },\n  { id: \"company\", label: \"Company / Project\", type: \"text\", placeholder: \"Your company or project name\" },\n];\n\nexport default function CTA() {\n  const [formData, setFormData] = useState({ name: \"\", email: \"\", company: \"\", message: \"\" });\n  const [submitted, setSubmitted] = useState(false);\n\n  const handleSubmit = (e: React.FormEvent) => {\n    e.preventDefault();\n    const existing = JSON.parse(localStorage.getItem(\"nq_leads\") || \"[]\");\n    existing.push({ ...formData, ts: new Date().toISOString() });\n    localStorage.setItem(\"nq_leads\", JSON.stringify(existing));\n    setSubmitted(true);\n  };\n\n  return (\n    <section\n      id=\"contact\"\n      className=\"relative py-32 overflow-hidden\"\n      style={{ background: \"var(--bg-dark)\" }}\n    >\n      {/* Atmospheric background */}\n      <div\n        className=\"absolute inset-0 pointer-events-none\"\n        style={{\n          background:\n            \"radial-gradient(ellipse at 30% 50%, rgba(124,58,237,0.12) 0%, transparent 60%), radial-gradient(ellipse at 80% 30%, rgba(139,92,246,0.06) 0%, transparent 50%)\",\n        }}\n      />\n\n      {/* Subtle grid */}\n      <div\n        className=\"absolute inset-0 pointer-events-none opacity-30\"\n        style={{\n          backgroundImage:\n            \"linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px)\",\n          backgroundSize: \"48px 48px\",\n        }}\n      />\n\n      <div className=\"section-container relative z-10\">\n        <div className=\"grid md:grid-cols-2 gap-16 items-start\">\n          {/* Left: Copy */}\n          <div>\n            <div className=\"flex items-center gap-3 mb-6\">\n              <div className
<truncated 7577 bytes>